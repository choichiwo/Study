package Class2;

public class Read {

	public static void main(String[] args) {
		/*
		 * 객체지향 언어의 특징
		 * 1. 캡슐화(Encapsulization)
		 * 	  클래스(객체)
		 *   접근제한자(Access Modifier)
		 *   public(무제한사용/공유)
		 *   protected(상속: 다른 페키지에 속한 자식클래스만 공유)
		 *   default(기본값)(같은 페키지 안에서 공유)
		 *   private(클래스 내부에서만 공유)-클래스의 필드
		 * 2.상속(Inheritance)
		 *   클래스의 속성을 다른 클래스가 물려받는것.
		 *   메소드 오버라이딩(Method Overriding) 클래스라서 명칭이달라도됨
		 *   생성자 오버라이딩(Constructor Overriding) 같은이름 메소드 무시
		 * 3.다형성(Polymophism)
		 *   메소드 오버로딩
		 *   생성자 오버로딩
		 * 4.추상화(Abstraction)
		 * 
		 *  super 위 superclass 상위클래스
		 *  sub 아래 subclass 하위클래스   
		 *  
		 *  final 수정불가(필드) 
		 *        상속불가(클래스)
		 *        상속가능,재정의불가(메소드)
		 *        
		 *  상속 : 부모클래스 => 자식클래스
		 *  타입 변환: 부모클래스 <= 자식클래스      
		 */

	}

}
